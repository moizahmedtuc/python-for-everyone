"""
A lambda function is an anonymous, small, inline function that can have
any number of arguments but can only have one expression.

lambda arguments: expression
"""
multiply = lambda x: x * 2
print(multiply(2))

add = lambda x, y: x + y
print(add(2, 3))
